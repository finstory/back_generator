export type Pos = { start: number, end: number };

export type TextPosition = { column: number, row: number };

export type Tags = `<ROUTES>` | `<SERVICES>` | `<CONTROLLERS>` | `<ENTITIES>` | `<INTERFACES>` | `<IMPORTS>` | `<EXPORTS>`;

